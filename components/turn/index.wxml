<!--components/turn/index.wxml-->
<view class="container">
  <image bind:tap="onLeft" class="icon-turn" src="{{last ? leftDisSrc : leftSrc}}" />
  <text class="title">{{title}}</text>
  <image bind:tap="onRight" class="icon-turn" src="{{first ? rightDisSrc : rightSrc}}" />
</view>
